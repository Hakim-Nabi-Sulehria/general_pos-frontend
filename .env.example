# Local dev (`npm run dev`) — copy to `.env` if you want to override defaults
VITE_API_URL=http://localhost:3000

# Production builds use `.env.production` (committed) or set `VITE_API_URL` in your host (e.g. Vercel) to override.
